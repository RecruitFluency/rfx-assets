{
  "name": "recruitfill-tests",
  "version": "1.0.0",
  "private": true,
  "description": "jsdom tests for the RFX RecruitRush matching/fill engine",
  "scripts": {
    "test": "node form-fill.test.js && node behaviors.test.js && node metrics.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
